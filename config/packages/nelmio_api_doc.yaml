nelmio_api_doc:
    documentation:
        host: devicetracker-env.sgxpxxsehq.eu-central-1.elasticbeanstalk.com
        schemes: [http]
        info:
            title: Device tracker KTU-iTo
            #description: This is an awesome app!
            version: 0.8.0
        #securityDefinitions:
         #   Bearer:
          #      type: apiKey
           #     description: 'Value: Bearer {jwt}'
            #    name: Authorization
             #   in: header
        #security:
         #   - Bearer: []

    areas: # to filter documented areas
        path_patterns:
            #- ^/api(?!/doc$) # Accepts routes under /api except /api/doc
            #- ^/
            - ^/(?!(api/doc|account/all)) # All routes except those starting with api/doc
            #- ^/(?!account/all) # Exlude endpoint used for testing
